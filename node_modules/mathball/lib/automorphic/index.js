'use strict';

/* Automorphic Number
 * Function: isAutomorphic()
 */

var validate = require('../validation/positive-integer');

exports.check = function (num) {
	validate(num, 'isAutomorphic');
	var str = Math.pow(num, 2).toString();
	return num == str.slice(str.indexOf(num.toString()));
};
'use strict';

/* Magic Number
 * Function: magic()
 */

var validate = require('../validation/positive-integer');

exports.find = function (num) {
	validate(num, 'magic');
	var pow = 1,
	    res = 0;
	while (num) {
		pow *= 5;
		res += num & 1 ? pow : 0;
		num >>= 1;
	}
	return res;
};